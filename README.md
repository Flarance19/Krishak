# Krishak Backend

This is the backend for the Krishak website, built with Node.js, Express, and MongoDB.

## Setup

1. Install dependencies:
   ```
npm install
   ```
2. Start MongoDB locally or update `.env` with your MongoDB URI.
3. Run the server:
   ```
npm run dev
   ```

The server will run on `http://localhost:5000` by default.
